---
import ThemeToggle from "./ThemeToggle.astro";
import es from "../i18n/es";
import en from "../i18n/en";
import type { Language } from "../types/language.type";

const url = Astro.url.pathname;
const lang = Astro.params.lang as Language;
const translations:any = { es, en };
const t = translations[lang];


const languages = [
  { code: "es", label: t.languageOptions.es },
  { code: "en", label: t.languageOptions.en },
];



---

<header class="w-full bg-white dark:bg-gray-900 shadow">
  <nav class="max-w-6xl mx-auto flex items-center justify-between p-4">
    <a href="/" class="text-xl font-bold">{t.nav.portfolio}</a>

    <div class="flex items-center gap-6">
      <ul class="flex gap-6 text-sm font-medium">
        <li><a href={`/${lang}/ `} class={'hover:text-cyan-500'} >{t.nav.home}</a></li>
        <li><a href={`/${lang}/projects`} class={'hover:text-cyan-500'} >{t.nav.projects}</a></li>
        <li><a href={`/${lang}/about`} class={'hover:text-cyan-500'} >{t.nav.about_me}</a></li>
      </ul>

      <form id="lang-form">
        <select id="lang-select" class="dark:bg-gray-800 cursor-pointer p-1 rounded focus:outline-none ring-1 ring-transparent cursor-pointer hover:ring-gray-300 dark:hover:ring-gray-600">
          {
            languages.map((langOpt) => (
              <option value={langOpt.code} selected={langOpt.code === lang}>
                {langOpt.label}
              </option>
            ))
          }
        </select>
      </form>

      <ThemeToggle></ThemeToggle>
    </div>
  </nav>
</header>
